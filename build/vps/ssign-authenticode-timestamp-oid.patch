Fix the OID under which the RFC3161 token is embedded, so Windows sees it.

ssign 0.1.1 puts the timestamp token in the SignerInfo's unauthenticatedAttributes
under 1.2.840.113549.1.9.16.2.14 (id-aa-timeStampToken — the generic CMS/CAdES
attribute). Authenticode does not read that attribute. Windows requires the
Microsoft-specific 1.3.6.1.4.1.311.3.3.1 (szOID_RFC3161_counterSign).

The consequence is silent and nasty: ssign reports success, the TSA really is
contacted, the token really is in the file — and Get-AuthenticodeSignature reports
TimeStamperCertificate = null while osslsigncode says "Timestamp is not available".
The signature is valid today and turns invalid the moment the signing certificate
expires (2027-05-17), including on binaries already shipped.

Verified end-to-end against upstream f4fc7d7aca180982a11d9be4ee5aebd394ad5545: with
this one-line change Windows reports the countersignature from "Certum Timestamp
2026" (valid to 2036), and osslsigncode reports "Timestamp verified".

diff --git a/ssign-core/src/authenticode.rs b/ssign-core/src/authenticode.rs
index 2f548a7..6d1fe81 100644
--- a/ssign-core/src/authenticode.rs
+++ b/ssign-core/src/authenticode.rs
@@ -23,7 +23,7 @@ const OID_SPC_INDIVIDUAL: &str = "1.3.6.1.4.1.311.2.1.21";
 const OID_CONTENT_TYPE: &str = "1.2.840.113549.1.9.3";
 const OID_MESSAGE_DIGEST: &str = "1.2.840.113549.1.9.4";
 const OID_SIGNING_TIME: &str = "1.2.840.113549.1.9.5";
-const OID_TIMESTAMP_TOKEN: &str = "1.2.840.113549.1.9.16.2.14";
+const OID_TIMESTAMP_TOKEN: &str = "1.3.6.1.4.1.311.3.3.1";

 /// The `SpcAttributeTypeAndOptionalValue` is identical for every PE signature
 /// (SPC_PE_IMAGE_DATA + the "<<<Obsolete>>>" moniker); captured verbatim.
